# 🔐 دليل تسجيل الدخول إلى منصة فرصة

## ✅ تم إصلاح المشكلة!

تم اكتشاف وإصلاح خطأ في الكود البرمجي كان يمنع تحميل API بشكل صحيح:

### المشكلة:
- استخدام `??` (null coalescing operator) الذي غير مدعوم في PHP 5.6
- الخطأ كان في ملف `php/includes/functions.php` على السطور: 518, 544, 545, 579, 580, 581, 582

### الحل:
✅ تم استبدال جميع استخدامات `??` بصيغة متوافقة مع PHP 5.6 باستخدام `isset()`

---

## 👥 حسابات الاختبار الجاهزة

استخدم أحد هذه الحسابات لتسجيل الدخول:

### 1️⃣ **باحث عن عمل (Job Seeker)**
```
البريد الإلكتروني: jobseeker@farsa.ye
كلمة المرور: Password123!
نوع الحساب: باحث عن عمل
```

### 2️⃣ **صاحب عمل (Employer)**
```
البريد الإلكتروني: employer@farsa.ye
كلمة المرور: Password123!
نوع الحساب: صاحب عمل
```

### 3️⃣ **مسؤول المنصة (Admin)**
```
البريد الإلكتروني: admin@farsa.ye
كلمة المرور: Password123!
نوع الحساب: مسؤول
```

---

## 🚀 كيفية تسجيل الدخول

### الخطوة 1: اذهب لصفحة تسجيل الدخول
```
http://localhost/fursa/pages/auth/login.php
```

### الخطوة 2: أدخل بيانات الحساب
- اختر حساب من الحسابات أعلاه
- أدخل البريد الإلكتروني
- أدخل كلمة المرور

### الخطوة 3: اضغط "تسجيل الدخول"

### الخطوة 4: سيتم نقلك لـ لوحة التحكم
- إذا كنت باحث عن عمل: `/pages/dashboard/job-seeker/index.php`
- إذا كنت صاحب عمل: `/pages/dashboard/employer/index.php`
- إذا كنت مسؤول: `/pages/dashboard/admin/index.php`

---

## ❌ حل المشاكل الشائعة

### المشكلة 1: "حدث خطأ في الاتصال"

**السبب:** عادة يكون خطأ في الاتصال بـ Apache أو MySQL

**الحل:**
1. ✅ تأكد من تشغيل Apache
2. ✅ تأكد من تشغيل MySQL
3. ✅ تأكد من استيراد قاعدة البيانات `database.sql`
4. ✅ امسح التخزين المؤقت (Cache) في المتصفح: `Ctrl+Shift+Delete`
5. ✅ أعد تحميل الصفحة: `Ctrl+Shift+R`

---

### المشكلة 2: "البريد أو كلمة المرور غير صحيحة"

**السبب:** بيانات الحساب غير صحيحة

**الحل:**
1. تأكد من كتابة البريد بشكل صحيح
2. تأكد من كتابة كلمة المرور بشكل صحيح (تذكر أن حالة الأحرف مهمة)
3. استخدم أحد الحسابات المدرجة أعلاه

---

### المشكلة 3: لا شيء يحدث عند الضغط على الزر

**السبب:** مشكلة في الاتصال بالخادم

**الحل:**
1. افتح أدوات المطور: `F12`
2. اذهب إلى عرض الشبكة: `Network`
3. حاول تسجيل الدخول مرة أخرى
4. ستجد طلب API يسمى `api_login.php`
5. تحقق من حالة الاتصال:
   - ✅ 200 = نجح
   - ❌ 500 = خطأ في الخادم
   - ❌ 0 = لا اتصال

---

## 📊 التحقق من قاعدة البيانات

### للتحقق من أن كل شيء يعمل:

```
http://localhost/fursa/check_db.php
```

يجب أن ترى:
```
✓ Connected to: localhost
✓ Database: farsa_db
✓ User: root
✓ Users table exists: 3 users
```

---

## 🔄 إعادة تعيين كلمات المرور

إذا نسيت كلمة المرور:

### الطريقة 1: استخدم حساب الاختبار
استخدم كلمة المرور الموجودة أعلاه: `Password123!`

### الطريقة 2: أعد تعيين يدوياً من PHPMyAdmin
1. اذهب إلى: `http://localhost/phpmyadmin`
2. اختر قاعدة البيانات: `farsa_db`
3. اختر الجدول: `users`
4. عدّل كلمة المرور
5. استخدم دالة: `MD5()` أو `bcrypt()`

---

## 💡 نصائح مهمة

### ✅ ما يجب فعله:
- استخدم حسابات الاختبار المرفوعة أعلاه
- تحقق من أن MySQL قيد التشغيل
- استخدم المتصفح الحديث (Chrome, Firefox, Edge)
- امسح التخزين المؤقت عند حدوث مشاكل

### ❌ ما يجب تجنبه:
- لا تستخدم أحرف خاصة في كلمة المرور (في البداية)
- لا تترك حقول فارغة
- لا تغلق MySQL أثناء العمل
- لا تحذف ملفات البيانات

---

## 📞 هل تحتاج إلى مساعدة إضافية؟

### خطوات الاستكشاف:
1. افتح `logs/error.log` لرؤية الأخطاء
2. استخدم أدوات المطور في المتصفح (F12)
3. جرب تسجيل الدخول من متصفح مختلف
4. امسح كل التخزين المؤقت والـ Cookies

---

## ✨ شاهد ميزات المنصة

بعد تسجيل الدخول، استكشف:

### للباحثين عن عمل:
- 🔍 البحث عن الوظائف
- 📋 عرض تفاصيل الوظيفة
- 📝 التقديم على الوظائف
- 💾 حفظ الوظائف المفضلة
- ⭐ تقييم الشركات

### لأصحاب العمل:
- 📝 نشر وظائف جديدة
- 👥 إدارة المتقدمين
- 📊 إحصائيات الوظائف
- 💬 التواصل مع المتقدمين

### للمسؤولين:
- 👤 إدارة المستخدمين
- 🏢 إدارة الشركات
- 📊 إحصائيات المنصة
- 🔒 إدارة الأمان

---

**آخر تحديث:** 28 نوفمبر 2025
